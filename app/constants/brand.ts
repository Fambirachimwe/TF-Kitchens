import logoSrc from "~/data/logo.png";

/** Vite-resolved logo URL for components */
export const LOGO_SRC = logoSrc;

/** Public URL for favicon, Open Graph, and static references */
export const LOGO_PUBLIC_PATH = "/logo.png";
